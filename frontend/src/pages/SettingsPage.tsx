//displays the user settings
import { useScrollReveal } from "../hooks/useScrollReveal";

export default function SettingsPage() {
  useScrollReveal();

  return (
    <div>
      <h2 data-reveal>Settings</h2>
      {/*design a settings page*/}
    </div>
  );
}
