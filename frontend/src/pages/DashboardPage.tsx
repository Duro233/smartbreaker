//Display linked devices, show realtime metrics, option to flip breaker and remove it.

export default function DashboardPage() {
  return (
    <div>
      <h2>Dashboard</h2>
      {/*graph for readings*/}
      {/*show linked devices*/}
      <button>Toogle Switch</button>
    </div>
  );
}
